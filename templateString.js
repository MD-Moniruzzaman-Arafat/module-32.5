// const name = 'Arafat';
const friendInfo = {
    name: 'salman',
    id: 20212203007,
    intake: 38
}

// destructering object
const { name, id, intake } = friendInfo

const result = `hello my name is ${name} . my id is ${id} .`;
console.log(result);